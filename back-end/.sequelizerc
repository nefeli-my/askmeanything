const path = require('path');

module.exports = {
    "config": path.resolve('./authenticator/server/config', 'config.js'),
    "models-path": path.resolve('./authenticator/server/models'),
    "seeders-path": path.resolve('./authenticator/server/seeders'),
    "migrations-path": path.resolve('./authenticator/server/migrations')
};